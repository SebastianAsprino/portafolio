<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Developer Portfolio</title>
<script is:inline defer src="https://umami.asprino.dev/script.js" data-website-id="74708092-3278-4e33-9139-54904dcd12f9"></script>

    <style>
        :root {
            --bg-color: #f9f4ff;
            --card-color: rgba(255, 255, 255, 0.8);
            --text-color: #333;
            --accent-color: #8b5cf6;
            --secondary-color: #ec4899;
            --border-color: rgba(255, 255, 255, 0.5);
            --shadow-color: rgba(0, 0, 0, 0.1);
        }

        .dark-theme {
            --bg-color: #111827;
            --card-color: rgba(31, 41, 55, 0.8);
            --text-color: #f3f4f6;
            --border-color: rgba(55, 65, 81, 0.5);
            --shadow-color: rgba(0, 0, 0, 0.3);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: var(--bg-color);
            background: linear-gradient(135deg, #f9f4ff 0%, #e0f2fe 100%);
            color: var(--text-color);
            transition: all 0.5s ease;
            padding: 20px;
            min-height: 100vh;
        }

        body.dark-theme {
            background: linear-gradient(135deg, #111827 0%, #1f2937 100%);
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        /* Theme Toggle */
        .theme-toggle {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 100;
            background: var(--card-color);
            backdrop-filter: blur(10px);
            border: 1px solid var(--border-color);
            border-radius: 50px;
            padding: 8px 16px;
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            box-shadow: 0 4px 20px var(--shadow-color);
            transition: all 0.3s ease;
        }

        .theme-toggle:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 25px var(--shadow-color);
        }

        /* Header */
        .header {
            text-align: center;
            margin: 40px 0 60px;
        }

        .header h1 {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 16px;
            background: linear-gradient(90deg, var(--accent-color), var(--secondary-color));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        @media (min-width: 768px) {
            .header h1 {
                font-size: 3.5rem;
            }
        }

        .header p {
            font-size: 1.1rem;
            max-width: 600px;
            margin: 0 auto;
            opacity: 0.8;
            line-height: 1.6;
        }

        /* Bento Grid */
        .bento-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 20px;
            margin-bottom: 60px;
        }

        @media (min-width: 768px) {
            .bento-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (min-width: 1024px) {
            .bento-grid {
                grid-template-columns: repeat(3, 1fr);
            }
        }

        .bento-card {
            background: var(--card-color);
            backdrop-filter: blur(10px);
            border: 1px solid var(--border-color);
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 4px 20px var(--shadow-color);
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .bento-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px var(--shadow-color);
        }

        .bento-card h2 {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 16px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .bento-card h3 {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 12px;
        }

        .bento-card p {
            line-height: 1.6;
            margin-bottom: 16px;
            opacity: 0.9;
        }

        /* Grid Item Sizes */
        .span-2 {
            grid-column: span 2;
        }

        @media (max-width: 767px) {
            .span-2 {
                grid-column: span 1;
            }
        }

        /* Progress Bar */
        .progress-container {
            margin: 16px 0;
        }

        .progress-label {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            font-size: 0.9rem;
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background: rgba(255, 255, 255, 0.3);
            border-radius: 4px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, var(--accent-color), var(--secondary-color));
            border-radius: 4px;
        }

        /* Skill Tags */
        .skills-container {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin-top: 16px;
        }

        .skill-tag {
            background: rgba(139, 92, 246, 0.1);
            color: var(--accent-color);
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.85rem;
            font-weight: 500;
        }

        .dark-theme .skill-tag {
            background: rgba(139, 92, 246, 0.2);
            color: #c4b5fd;
        }

        /* Project Grid */
        .project-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 12px;
            margin-top: 16px;
        }

        .project-item {
            aspect-ratio: 1;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.9rem;
            text-align: center;
            padding: 8px;
            background: linear-gradient(45deg, #8b5cf6, #ec4899);
            color: white;
        }

        /* Stats */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 16px;
            margin-top: 20px;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: 800;
            margin-bottom: 8px;
            background: linear-gradient(90deg, var(--accent-color), var(--secondary-color));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .stat-label {
            font-size: 0.9rem;
            opacity: 0.8;
        }

        /* Footer */
        .footer {
            text-align: center;
            padding: 40px 0 20px;
            font-size: 0.9rem;
            opacity: 0.7;
        }

        /* Animations */
        @keyframes float {
            0% { transform: translateY(0px); }
            50% { transform: translateY(-10px); }
            100% { transform: translateY(0px); }
        }

        .floating {
            animation: float 3s ease-in-out infinite;
        }

        /* Responsive Adjustments */
        @media (max-width: 767px) {
            .header {
                margin: 30px 0 40px;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .bento-card {
                padding: 20px;
            }
            
            .stats-container {
                grid-template-columns: 1fr;
                gap: 12px;
            }
        }
    </style>
</head>
<body>
    <!-- Theme Toggle -->
    <button class="theme-toggle" id="themeToggle">
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" id="themeIcon">
            <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
        </svg>
        <span id="themeText">Dark Mode</span>
    </button>

    <div class="container">
        <!-- Header -->
        <header class="header">
            <h1>Developer Portfolio</h1>
            <p>Full-stack developer with a passion for creating beautiful, functional experiences.</p>
        </header>

        <!-- Bento Grid -->
        <div class="bento-grid">
            <!-- Welcome Section -->
            <div class="bento-card span-2">
                <h2>👋 Welcome to My Portfolio</h2>
                <p>I'm a full-stack developer who loves building things from the ground up. This portfolio showcases my skills, projects, and interests.</p>
                <p>Feel free to explore my work below. I'm always excited to take on new challenges!</p>
            </div>

            <!-- Skills Section -->
            <div class="bento-card">
                <h3>🛠️ Technical Skills</h3>
                <p>Frontend, Backend, Database, and DevOps technologies I work with:</p>
                <div class="skills-container">
                    <span class="skill-tag">React</span>
                    <span class="skill-tag">Node.js</span>
                    <span class="skill-tag">MongoDB</span>
                    <span class="skill-tag">PostgreSQL</span>
                    <span class="skill-tag">Docker</span>
                    <span class="skill-tag">AWS</span>
                </div>
            </div>

            <!-- Current Projects -->
            <div class="bento-card">
                <h3>🚀 Current Projects</h3>
                <div class="progress-container">
                    <div class="progress-label">
                        <span>Project Alpha</span>
                        <span>65%</span>
                    </div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 65%;"></div>
                    </div>
                </div>
                <div class="progress-container">
                    <div class="progress-label">
                        <span>Project Beta</span>
                        <span>85%</span>
                    </div>
                    <div class="progress-bar">
                        <div class="progress-fill" style="width: 85%;"></div>
                    </div>
                </div>
            </div>

            <!-- Recent Work -->
            <div class="bento-card">
                <h3>🎨 Recent Work</h3>
                <div class="project-grid">
                    <div class="project-item">Project 1</div>
                    <div class="project-item">Project 2</div>
                    <div class="project-item">Project 3</div>
                    <div class="project-item">Project 4</div>
                </div>
            </div>

            <!-- Contact -->
            <div class="bento-card">
                <h3>📬 Get In Touch</h3>
                <p>I'm always open to new opportunities and collaborations!</p>
                <p>Email: your.email@example.com</p>
                <p>GitHub: github.com/yourusername</p>
                <p>LinkedIn: linkedin.com/in/yourprofile</p>
            </div>

            <!-- About Me -->
            <div class="bento-card span-2">
                <h3>🤔 About Me</h3>
                <div style="display: grid; grid-template-columns: 1fr; gap: 20px; align-items: center;">
                    <div>
                        <p>I'm a passionate full-stack developer with X years of experience building web applications. I love solving complex problems and creating intuitive user experiences.</p>
                        <p>When I'm not coding, you can find me exploring new technologies, contributing to open source, or enjoying outdoor activities.</p>
                    </div>
                    <div style="text-align: center; padding: 20px;">
                        <div style="width: 120px; height: 120px; background: linear-gradient(45deg, #8b5cf6, #ec4899); border-radius: 50%; margin: 0 auto; display: flex; align-items: center; justify-content: center; font-size: 2rem;">👨‍💻</div>
                    </div>
                </div>
            </div>

            <!-- Stats -->
            <div class="bento-card">
                <h3>📊 Stats</h3>
                <div class="stats-container">
                    <div class="stat-item">
                        <div class="stat-number">50+</div>
                        <div class="stat-label">Projects Completed</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">3+</div>
                        <div class="stat-label">Years Experience</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-number">20+</div>
                        <div class="stat-label">Happy Clients</div>
                    </div>
                </div>
            </div>

            <!-- Testimonials -->
            <div class="bento-card span-2">
                <h3>💬 Testimonials</h3>
                <div style="display: grid; grid-template-columns: 1fr; gap: 16px; margin-top: 16px;">
                    <div style="background: rgba(255, 255, 255, 0.1); padding: 16px; border-radius: 12px; border: 1px solid var(--border-color);">
                        <p style="font-style: italic; margin-bottom: 8px;">"Working with this developer was an absolute pleasure. They delivered beyond expectations!"</p>
                        <p style="font-weight: 600;">- Client Name, Company</p>
                    </div>
                    <div style="background: rgba(255, 255, 255, 0.1); padding: 16px; border-radius: 12px; border: 1px solid var(--border-color);">
                        <p style="font-style: italic; margin-bottom: 8px;">"The attention to detail and technical expertise is unmatched. Highly recommend!"</p>
                        <p style="font-weight: 600;">- Another Client, Organization</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <footer class="footer">
            <div>Created with ❤️ • <span id="currentDate"></span></div>
        </footer>
    </div>

    <script>
        // Theme Toggle
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');
        const themeText = document.getElementById('themeText');
        
        // Check for saved theme preference or use system preference
        const savedTheme = localStorage.getItem('theme') || 
            (window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');
        
        if (savedTheme === 'dark') {
            document.body.classList.add('dark-theme');
            themeIcon.innerHTML = '<path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" clip-rule="evenodd"></path>';
            themeText.textContent = 'Light Mode';
        }
        
        themeToggle.addEventListener('click', () => {
            document.body.classList.toggle('dark-theme');
            
            if (document.body.classList.contains('dark-theme')) {
                // Switch to dark theme icon (moon)
                themeIcon.innerHTML = '<path fill-rule="evenodd" d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" clip-rule="evenodd"></path>';
                themeText.textContent = 'Light Mode';
                localStorage.setItem('theme', 'dark');
            } else {
                // Switch to light theme icon (sun)
                themeIcon.innerHTML = '<path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>';
                themeText.textContent = 'Dark Mode';
                localStorage.setItem('theme', 'light');
            }
        });

        // Set current date
        const now = new Date();
        const options = { year: 'numeric', month: 'long', day: 'numeric' };
        document.getElementById('currentDate').textContent = now.toLocaleDateString('en-US', options);
    </script>
</body>
</html>